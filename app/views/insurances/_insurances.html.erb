<% @insurances.each do |insurance| %>
	<tr>								
		<th>
			<%=
				link_to insurance.name, 
				edit_insurance_path(insurance)
			%></th>
		<th>
			<%=
				link_to insurance.identifier,
					edit_insurance_path(insurance)
			%>
		</th>
		<th><%= insurance.city %></th>
		<th><%= insurance.state %></th>		
		<th><%= insurance.status %></th>
	</tr>
<% end %>